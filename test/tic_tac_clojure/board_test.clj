(ns tic-tac-clojure.board-test
  (:require [clojure.test :refer :all]
            [tic-tac-clojure.board :refer :all]))


            
; (deftest sample-test
;   (is (= 1 (sample))))

; (deftest sample-test
;   (is (= 4 (+ 2 2)) "sample test"))